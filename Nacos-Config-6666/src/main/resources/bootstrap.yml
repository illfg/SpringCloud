
server:
  port: 6666
spring:
  application:
    name: nacos-config

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

      config:
        file-extension: yml #指定yml格式的配置
        server-addr: 127.0.0.1:8848
  profiles:
    active: dev
